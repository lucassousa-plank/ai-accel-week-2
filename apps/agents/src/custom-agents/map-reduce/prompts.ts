/**
 * Default prompts.
 */

export const subjectsPrompt = `Generate a comma separated list of between 2 and 5 examples related to: {topic}.`

export const jokePrompt = `Generate a joke about {subject}`

export const bestJokePrompt = `Below are a bunch of jokes about {topic}. Select the best one! Return the ID (index) of the best one.

{jokes}`